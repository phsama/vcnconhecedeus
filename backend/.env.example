# SMTP (leave empty to use dev mode — link logged to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@vocenahconhecedeus.com.br

# Frontend base URL (for redirect links in emails)
PUBLIC_BASE_URL=http://localhost:3000

# Admin credentials for CSV export
ADMIN_USER=admin
ADMIN_PASS=changeme

# Database (SQLite file path)
DATABASE_URL=sqlite:///./data/subscribers.db
